/*
* 자바스크립트에는 타입이 존재한다
* 선언 시에는 타입을 지정하지 않아도 된다
* 그러나 형변환이 필요할 때도 있다
* 타입은 Number, Strings, Boolean, undefied, null, Symbol, Object()
* Object() : 객체 (array[], object{}, function())
* 데이터 타입 확인 명령어 : typeof
* 
*/

const age = 23;
const temp = -10;
const pi = 3.14;

console.log(typeof age);
console.log(typeof temp);
console.log(typeof pi);